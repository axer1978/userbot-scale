[pytest]
testpaths = tests
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
filterwarnings =
    ignore::DeprecationWarning
markers =
    requires_pg: needs a real Postgres reachable at PG_TEST_DSN (skipped otherwise)
